# Webpack Dev Server 示例

本示例演示 webpack-dev-server 的使用和配置。

## 运行步骤

1. 安装依赖：`npm install`
2. 启动开发服务器：`npm start`
3. 浏览器会自动打开 http://localhost:8080
4. 修改 `src/index.js` 或 `src/styles.css`，观察浏览器自动刷新

## 预期结果

- 开发服务器在 8080 端口启动
- 修改代码后浏览器自动刷新
- 支持实时重新加载
