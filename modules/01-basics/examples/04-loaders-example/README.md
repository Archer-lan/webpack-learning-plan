# Loader 使用示例

本示例演示如何使用常用的 loader 处理 CSS、图片等资源。

## 运行步骤

1. 安装依赖：
```bash
npm install
```

2. 运行打包：
```bash
npm run build
```

3. 在浏览器中打开 `dist/index.html` 查看效果

## 预期结果

- CSS 样式被正确应用
- 图片资源被正确加载
- 生成的文件包含所有处理后的资源
