# Plugin 使用示例

本示例演示常用 plugin 的使用，包括 HtmlWebpackPlugin、MiniCssExtractPlugin 等。

## 运行步骤

1. 安装依赖：`npm install`
2. 运行打包：`npm run build`
3. 在浏览器中打开 `dist/index.html` 查看效果

## 预期结果

- 自动生成 HTML 文件并注入资源
- CSS 被提取到单独的文件
- 输出目录被自动清理
